
#this is here for refrence use at own descression
# module "aws_dev_secrets" {
#   source  = "modules/terraform-aws-secretsmanager"
#   secret_id = "${var.jenkins_aws_secrets_arn}"
# }

# locals {
#   jenkins_username = "${local.jenkins_username}"
#   jenkins_password = "${local.jenkins_password}"
# }
